#!/usr/bin/env python3

hostname = input("What should the hostname be set to? ")
if hostname.lower() == "mtg":
    print("The hostname was found to be MTG")
    print("The hostname matches expected config")

print("Exiting script")

